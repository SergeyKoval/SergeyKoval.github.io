import { FilteredSelectedHotelDirective } from './filtered-selected-hotel.directive';

describe('FilteredSelectedHotelDirective', () => {
  it('should create an instance', () => {
    const directive = new FilteredSelectedHotelDirective();
    expect(directive).toBeTruthy();
  });
});
