import { FilteredHotelsDirective } from './filtered-hotels.directive';

describe('FilteredHotelsDirective', () => {
  it('should create an instance', () => {
    const directive = new FilteredHotelsDirective();
    expect(directive).toBeTruthy();
  });
});
